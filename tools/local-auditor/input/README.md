# Local CSV Drop

Place Restroworks exports here only when running on the approved local machine.
CSV/XLS files, screenshots, normalized rows, and private review packets are
ignored by Git and must not be uploaded.

Prefer passing an external drop folder to `run_full_pipeline.bat` so operational
data remains outside the repository working tree.
