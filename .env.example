# GitHub Pages contains no runtime secrets.
#
# Copy `supabase/.env.example` to `supabase/.env` only for local Edge Function
# development. Production values must be entered in Supabase Edge Function
# Secrets and must never be committed.
